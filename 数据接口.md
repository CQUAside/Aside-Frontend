# aside前后端接口联调

## 上传广告页面

### form表单

#### 搜索广告

```
action: searchAdsController
data: {
    '搜索词': 'keyword'
}
```

#### 用户上传广告数据

```
action: uploadAdController
data: {
    '标题': 'adTitle',
    '图片': 'adImgSrc',
    '描述': 'adDescription',
    '投放区域': 'adType',
    '投放开始时间': 'adDisplayStartDate',
    '投放终止时间': 'adDisplayEndDate',
    '是否列表优先': 'adListPriority|0(否)/1(是)',
    '是否开通轮播': 'adEnableSlide|0(否)/1(是)'
}
```

### ajax

#### 获取所在城市

```
url: getCity
method: post
request parameter:
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'city': [city]
}
```

## 个人广告列表页面

### form表单

#### 搜索广告

```
action: searchAdsController
data: {
    '搜索词': 'keyword'
}
```

### ajax

#### 获取所在城市

```
url: getCity
method: post
request parameter:
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'city': [city]
}
```

#### 获取个人广告列表

```
url: getSelfAdsList
method: post
request parameter: {
    memberId: [memberId]',
    pageSize: [pageSize],
    pageNo: [pageNo],
    pageSum: [pageSum]
}
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'result': [
        {
            'adId': [adId],
            'adType': [adType],
            'adTitle': [adTitle],
            'adImgSrc': [adImgSrc],
            'adSender': [adSender],
            'adDescription': [adDescription]
        },
        {
            'adId': [adId],
            'adType': [adType],
            'adTitle': [adTitle],
            'adImgSrc': [adImgSrc],
            'adSender': [adSender],
            'adDescription': [adDescription]
        }
    ]
}
```

## 已登录首页

### form表单

#### 搜索广告

```
action: searchAdsController
data: {
    '搜索词': 'keyword'
}
```

### ajax

#### 获取所在城市

```
url: getCity
method: post
request parameter:
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'city': [city]
}
```

#### 导航

```
url: getMenu
method: post
request parameter:
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'result': [
        {
            'adTypeId': [adTypeId],
            'adType': [adType]
        }
    ]
}
```

#### 轮番

```
url: getSlides
method: post
request parameter:
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'result': [
        {
            'slideId': [slideId],
            ['slideText': [slideText],]
            'slideImgSrc': [slideImgSrc]
        }
    ]
}
```

#### 获取广告列表

```
url: getAdsList
method: post
request parameter: {
    pageSize: [pageSize],
    pageNo: [pageNo],
    pageSum: [pageSum]
}
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'result': [
        {
            'adId': [adId],
            'adType': [adType],
            'adTitle': [adTitle],
            'adImgSrc': [adImgSrc],
            'adSender': [adSender],
            'adDescription': [adDescription],
            'adPublishDate': [adPublishDate],
            'adClickCount': [adClickCount]
        },
        {
            'adId': [adId],
            'adType': [adType],
            'adTitle': [adTitle],
            'adImgSrc': [adImgSrc],
            'adSender': [adSender],
            'adDescription': [adDescription],
            'adPublishDate': [adPublishDate],
            'adClickCount': [adClickCount]
        }
    ]
}
```

#### 猜你喜欢

```
url: getRecommenderList
method: post
request parameter: {
    memberId: [memberId]
}
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'result': [
        {
            'adId': [adId],
            'adTitle': [adTitle],
            'adType': [adType],
            'adImgSrc': [adImgSrc],
            'adDescription': [adDescription]
        }
    ]
}
```

## 广告详情页面

### form表单

#### 搜索广告

```
action: searchAdsController
data: {
    '搜索词': 'keyword'
}
```

### ajax

#### 获取所在城市

```
url: getCity
method: post
request parameter:
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'city': [city]
}
```

#### 广告详情

```
url: getAdDescription
method: post
request parameter: {
    adId: [adId]
}
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'result': {
            'adId': [adId],
            'adType': [adType],
            'adTitle': [adTitle],
            'adImgSrc': [adImgSrc],
            'adSender': [adSender],
            'adDescription': [adDescription]
    }
}
```

#### 收藏

```
url: likeAd
method: post
request parameter: {
    memberId: [memberId],
    adId: [adId]
}
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'result': 0(未收藏)/1(已收藏)
}
```

## 控制面板_我的发布

### form表单

#### 搜索广告

```
action: searchAdsController
data: {
    '搜索词': 'keyword'
}
```

### ajax

#### 获取所在城市

```
url: getCity
method: post
request parameter:
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'city': [city]
}
```

#### 我的上传

```
url: getAdsList
method: post
request parameter: {
    memberId: [memberId],
    pageSize: [pageSize],
    pageNo: [pageNo],
    pageSum: [pageSum]
}
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'result': [
        {
            'adId': [adId],
            'adStatus': -1(审核失败)/0(未审核/待审核)/1(审核成功),
            'adType': [adType],
            'adTitle': [adTitle],
            'adImgSrc': [adImgSrc],
            'adSender': [adSender],
            'adDescription': [adDescription],
            'adDisplayStartDate': [adDisplayStartDate],
            'adDisplayEndDate': [adDisplayEndDate]
        },
        {
            'adId': [adId],
            'adStatus': -1(审核失败)/0(未审核/待审核)/1(审核成功),
            'adType': [adType],
            'adTitle': [adTitle],
            'adImgSrc': [adImgSrc],
            'adSender': [adSender],
            'adDescription': [adDescription],
            'adDisplayStartDate': [adDisplayStartDate],
            'adDisplayEndDate': [adDisplayEndDate]
        }
    ]
}
```

## 控制面板_我的收藏

### form表单

#### 搜索广告

```
action: searchAdsController
data: {
    '搜索词': 'keyword'
}
```

### ajax

#### 获取所在城市

```
url: getCity
method: post
request parameter:
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'city': [city]
}
```

#### 我的收藏

```
url: getLikesList
method: post
request parameter: {
    memberId: [memberId],
    pageSize: [pageSize],
    pageNo: [pageNo],
    pageSum: [pageSum]
}
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'result': [
        {
            'adId': [adId],
            'adType': [adType],
            'adTitle': [adTitle],
            'adImgSrc': [adImgSrc],
            'adSender': [adSender],
            'adDescription': [adDescription],
            'adDisplayStartDate': [adDisplayStartDate],
            'adDisplayEndDate': [adDisplayEndDate]
        },
        {
            'adId': [adId],
            'adType': [adType],
            'adTitle': [adTitle],
            'adImgSrc': [adImgSrc],
            'adSender': [adSender],
            'adDescription': [adDescription],
            'adDisplayStartDate': [adDisplayStartDate],
            'adDisplayEndDate': [adDisplayEndDate]
        }
    ]
}
```

## 注册页面1

### form表单

#### 搜索广告

```
action: searchAdsController
data: {
    '搜索词': 'keyword'
}
```

#### 用户注册数据

```
action: registController
data: {
    '用户名': 'username',
    '密码': 'pwd',
    '手机号': 'mobile'
}
```

### ajax

#### 获取所在城市

```
url: getCity
method: post
request parameter:
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'city': [city]
}
```

#### 获取手机验证码

```
url: getCaptcha
method: post
request parameter: {mobile: [mobile]'}
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'captcha': [captcha]
}
```

## 注册页面2_个人信息页面
### form表单

#### 搜索广告

```
action: searchAdsController
data: {
    '搜索词': 'keyword'
}
```

#### 更改个人信息

```
action: updateSelfInfo
data: {
    '用户名': 'username',
    '默认地址': 'address',
    '登录邮箱': 'email',
    '登陆密码': 'pwd'
}
```

### ajax

#### 获取所在城市

```
url: getCity
method: post
request parameter:
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'city': [city]
}
```

## 登陆页面

### form表单

#### 用户登陆数据

```
action: loginController
data: {
    '账号': 'username',
    '密码': 'pwd'
}
```

### ajax

无

## 轮播页详情页面

### form表单

#### 搜索广告

```
action: searchAdsController
data: {
    '搜索词': 'keyword'
}
```

### ajax

#### 获取所在城市

```
url: getCity
method: post
request parameter:
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'city': [city]
}
```

#### 轮番页详情广告列表

```
url: getSlideList
method: post
request parameter: {
    slideId: [slideId],
    pageSize: [pageSize],
    pageNo: [pageNo],
    pageSum: [pageSum]
}
response: {
    'status': 0(成功) / 其他值(失败),
    'msg': 失败的原因,
    'result': [
        {
            'adId': [adId],
            'adType': [adType],
            'adTitle': [adTitle],
            'adImgSrc': [adImgSrc],
            'adSender': [adSender],
            'adDescription': [adDescription]
        },
        {
            'adId': [adId],
            'adType': [adType],
            'adTitle': [adTitle],
            'adImgSrc': [adImgSrc],
            'adSender': [adSender],
            'adDescription': [adDescription]
        }
    ]
}
```





